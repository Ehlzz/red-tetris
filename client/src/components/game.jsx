const COLONNES = 10;
const LIGNES = 20;
const TOTAL_LIGNES = 24;

const grille = Array.from({ length: TOTAL_LIGNES }, () =>
    Array.from({ length: COLONNES }, () => 0)
    );

function game() {
  return (
    
    <>
      <div>Game Component</div>
        
        
    </>
  );
}

export default game;